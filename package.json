{
  "name": "babel-weight",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "execute": "npm run transpile && npm run non-transpile && npm run compare",
    "compare": "node ./compare.js",
    "non-transpile": "babel src -d dist/es6 --compact",
    "transpile": "babel src -d dist/es5 --presets es2015 --compact"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13"
  },
  "private": true
}
